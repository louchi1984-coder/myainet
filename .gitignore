# Python
__pycache__/
*.py[cod]

# macOS
.DS_Store

# 运行产物 / 每套部署各自的实例文件（跑脚本时生成，不是 skill 源码，含真实 IP/主机名，绝不提交）
network-raw.json
network-*.json
myaiweb-network-config.md
claude-windows-myaiweb-connect.md
*-pending-register.json
~/.myaiweb/

# 打包产物
*.skill

# 本地工具/编辑器配置
.claude/
.vscode/
.idea/

# 测试 / 调试产物（*.png 默认挡掉，防误传带真数据的截图；README 的演示图显式放行）
.playwright-mcp/
snapshot.md
*.png
!docs/dashboard.png
